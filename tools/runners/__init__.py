"""Command-line runners. Import the module, or invoke it as ``python -m``.

- ``tools.inspect_gds``: inventory cells and labels in a GDS.
- ``tools.extract_netlist``: reconstruct nets from geometry.
- ``tools.analyze_netlist``: cones, registers, and DOT output.
- ``tools.simulate_netlist``: play an input sequence.
- ``tools.check_influence``: exhaustive small-cone influence checks.
- ``tools.import_sky130_models``: refresh the official cell snapshot.
"""
